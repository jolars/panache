# Bracketed Spans

Simple span: [highlighted text]{.highlight}

With emphasis: [This is **bold** and *italic*]{ .styled}

With code: [The `function()` call]{.code-ref }

Multiple spans: [first]{.a} and [second]{.b} together.

Nested brackets: [[literal brackets]]{.example}

Complex attributes: [text]{#myid  .class1 .class2 key="value"}
